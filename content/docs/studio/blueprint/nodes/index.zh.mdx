---
title: 节点
description: 内置节点目录，每个创建浮窗分类一页，逐个列出节点签名
---

内置目录共 610 个节点，分布在 27 个分类中。分类就是你在创建浮窗里导航时看到的分组，因此这些页面的组织方式与编辑器一致

## 如何阅读一个节点

每个节点写成这样：

> **Delay**
>
> `blueprint.flow.delay` · Latent

标题下第一行是节点的 **type id**——存进文档的值，也是你从插件注册时要用的值。其后是该节点为真的标记：

| 标记 | 含义 |
| --- | --- |
| `Pure` | 无副作用。可用于 `function` 图|
| `Latent` | 会挂起执行直到完成。在 `function` 图中永远不可用|

`Pure` 本身**并不**意味着该节点能在 [Blueprint Value](/zh/docs/studio/blueprint) 里使用。那是另一条更窄的规则：节点必须同时是纯节点、非 latent，且属于允许的分类——Data、List、Math、Time、Text、Slider、Displayable、Element、Game——外加一小串特例（`Init` 与 `On Flush` 事件头、非 latent 的 Flow、本地变量读写、`Call Fn`、注释、`Return Value`，以及四个 Page 状态读取节点）。scope 限定为 `widgetMain` 的 Self 节点无论标记如何都被排除；在 Blueprint Value 里要触及某个控件，走 `Element` 字面量加 Element 分类的读取节点

然后是引脚表：

| 引脚 | 方向 | 类型 | 说明 |
| --- | --- | --- | --- |
| `in` | in · exec | — | |
| `duration` | in · data | `float` | 秒。可在节点卡上直接填字面量|
| `completed` | out · exec | — | 等待结束后触发|
| `token` | out · data | `Timer` | 接给 `Skip Delay` 可提前结束等待|

执行引脚传递执行，数据引脚传递值。*可在节点卡上直接填字面量*意味着你可以在卡片上输入该值，而不必拉一条连线。部分节点带**动态引脚**——由所选变量、所选函数签名或你追加的分支推导出来——这类引脚以文字说明，因为没有固定的表可列

## 可用性

节点出现在本目录里，不代表它会出现在你的创建浮窗中。有三层过滤：

- **图类型。** `function` 图拒绝副作用节点和 latent 节点
- **宿主与控件能力。** Self 节点只在具备该能力的控件蓝图里出现；Text 控件的蓝图里不会提供 `Set Slider Value`
- **已存在的绑定。** Element 节点只有在图中已有可兼容的 `Element`、`Element Flush` 或 `Element Click` 节点可供连接时才会出现

<Callout type="info">
存在多个兼容来源时，创建浮窗只列出该节点一次。若来源恰好唯一，添加时会自动连上；若有多个，则保留目标输入由你选择
</Callout>

## 分类

| 分类 | 节点数 | 涵盖 |
| --- | --- | --- |
| [Events](/zh/docs/studio/blueprint/nodes/events) | 52 | 入口点：生命周期、输入、Surface 转场、Broadcast|
| [Flow](/zh/docs/studio/blueprint/nodes/flow) | 13 | 分支、循环、顺序执行、延迟|
| [Functions](/zh/docs/studio/blueprint/nodes/functions) | 3 | 函数入口、调用、返回|
| [Variables](/zh/docs/studio/blueprint/nodes/variables) | 9 | 声明与读写蓝图变量、持久变量|
| [Data](/zh/docs/studio/blueprint/nodes/data) | 95 | 字面量、类型转换、类型判断、JSON、字符串、数组|
| [Math](/zh/docs/studio/blueprint/nodes/math) | 31 | 算术、取整、布尔逻辑、比较|
| [Time](/zh/docs/studio/blueprint/nodes/time) | 15 | 当前时间、日历，以及日期文本|
| [Game](/zh/docs/studio/blueprint/nodes/game) | 89 | 剧情控制、对话、偏好、存档、回顾|
| [App](/zh/docs/studio/blueprint/nodes/app) | 26 | Surface 导航、窗口、应用生命周期|
| [Localization](/zh/docs/studio/blueprint/nodes/localization) | 6 | 游戏面向玩家的语言|
| [Network](/zh/docs/studio/blueprint/nodes/network) | 3 | HTTP 请求与响应读取|
| [Displayable](/zh/docs/studio/blueprint/nodes/displayable) | 9 | 几何、外观变体、属性动画|
| [Element](/zh/docs/studio/blueprint/nodes/element) | 123 | 以上各类的 Element 版本：作用于接入的 `Element` 而非自身|
| [Image](/zh/docs/studio/blueprint/nodes/image) | 16 | 图片资源与填充|
| [Text](/zh/docs/studio/blueprint/nodes/text) | 28 | 文本内容与排版|
| [Text Input](/zh/docs/studio/blueprint/nodes/text-input) | 3 | 可编辑文本框|
| [Button](/zh/docs/studio/blueprint/nodes/button) | 7 | 按钮标签与指针|
| [Container](/zh/docs/studio/blueprint/nodes/container) | 6 | 容器布局与子元素|
| [Frame](/zh/docs/studio/blueprint/nodes/frame) | 8 | Frame 所展示的 Page 及其 params|
| [List](/zh/docs/studio/blueprint/nodes/list) | 33 | 运行时项、选中、滚动、项上下文|
| [Slider](/zh/docs/studio/blueprint/nodes/slider) | 9 | 值、范围与吸附|
| [Debug](/zh/docs/studio/blueprint/nodes/debug) | 2 | 日志与图内注释|
| [Input](/zh/docs/studio/blueprint/nodes/input) | 2 | 玩家在用哪一类设备，以及某个动作是不是被按住的 |
| [Sound](/zh/docs/studio/blueprint/nodes/sound) | 7 | 播放界面的音频片段，以及之后再找到它的句柄 |
| [Voice](/zh/docs/studio/blueprint/nodes/voice) | 5 | 播放录好的语音，以及当前生效的配音语言 |
| [Component](/zh/docs/studio/blueprint/nodes/component) | 1 | 把组件的这一次摆放与另一次区分开来的那个参数 |
| [Switch](/zh/docs/studio/blueprint/nodes/switch) | 9 | Switch 控件的选中状态，以及它的启用与可见开关 |
