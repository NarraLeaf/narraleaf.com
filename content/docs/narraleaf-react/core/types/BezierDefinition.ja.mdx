---
title: "BezierDefinition"
description: "カスタムイージングカーブを表す、4つの数値からなる3次ベジェ曲線の定義"
---
BezierDefinition は、3次ベジェ曲線を定義する4つの数値からなる配列です。この4つの数値は、曲線の2つの制御点の x、y 座標にそれぞれ対応します。

```typescript
type BezierDefinition = [number, number, number, number];
```
