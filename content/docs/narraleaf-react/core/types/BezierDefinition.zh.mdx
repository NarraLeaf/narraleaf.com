---
title: "BezierDefinition"
description: 由四个数字构成的三次贝塞尔曲线定义，用于描述自定义缓动曲线
---
BezierDefinition 是由四个数字组成的数组，用于定义一条三次贝塞尔曲线：这四个数字分别是曲线两个控制点的 x、y 坐标

```typescript
type BezierDefinition = [number, number, number, number];
```
