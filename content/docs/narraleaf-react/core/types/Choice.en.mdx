---
title: "Choice"
description: The prompt, actions, and `ChoiceConfig` that make up one resolved menu choice.
---
```typescript
type Choice = {
    action: Actions[];
    prompt: Sentence;
    config: ChoiceConfig;
};
```
