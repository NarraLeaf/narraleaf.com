---
title: "ユーティリティ"
description: 斜体、色、太字のテキスト用の i、c、b ワードフォーマットヘルパー関数
---
## Word

```typescript
import {i, c, b} from "narraleaf-react"
```

これらの関数は、それぞれ以下と等価です。

- `i` は `Word.italic` と等価です
- `c` は `Word.color` と等価です
- `b` は `Word.bold` と等価です
