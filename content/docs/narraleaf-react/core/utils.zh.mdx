---
title: "实用工具"
description: 用于斜体、颜色与加粗文字的 i、c、b 辅助函数
---
## Word

```typescript
import {i, c, b} from "narraleaf-react"
```

这些函数等同于以下内容：

- `i` 等同于 `Word.italic`
- `c` 等同于 `Word.color`
- `b` 等同于 `Word.bold`
